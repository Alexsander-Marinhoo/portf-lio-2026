/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental:{
    viewTransition: true
  },
  devIndicators: false,
  reactStrictMode: false,
  /* config options here */
};

export default nextConfig;
